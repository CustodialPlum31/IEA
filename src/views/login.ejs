<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - IEA</title>
    <link rel="stylesheet" href="https://bootswatch.com/5/pulse/bootstrap.min.css">
    <style>
        /* Estilo adicional para centrar el contenido verticalmente en la página */
        body, html {
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #f4c9f8; /* Color de fondo morado muy claro */
        }
        .container {
            width: 400px;
            background-color: #ffffff; /* Fondo blanco para el contenedor */
            padding: 20px;
            border-radius: 8px; /* Bordes redondeados */
            box-shadow: 0px 0px 10px rgba(0,0,0,0.1); /* Sombra ligera */
        }
        .form-group {
            margin-bottom: 1rem; /* Espaciado entre los campos del formulario */
        }
        .btn-login {
            background-color: #9a7ad5; /* Color de fondo morado claro para el botón */
            border: none;
            color: #ffffff; /* Texto blanco para contraste */
            width: 100%; /* Ancho completo */
        }
    </style>
</head>
<body>
    <div class="container">
        <h2 class="text-center mb-4">Iniciar sesión en IEA</h2>
        <form action="/login" method="POST">
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" class="form-control" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="password">Contraseña:</label>
                <input type="password" class="form-control" id="password" name="password" required>
            </div>
            <button type="submit" class="btn btn-login">Login</button>
        </form>
    </div>
</body>
</html>
